import { FadeInSectionDirective } from './fade-in-section.directive';

describe('FadeInSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInSectionDirective();
    expect(directive).toBeTruthy();
  });
});
